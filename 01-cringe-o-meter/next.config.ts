import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Dev-only on-screen overlay; disabled so it never shows up in recordings.
  devIndicators: false,
};

export default nextConfig;
